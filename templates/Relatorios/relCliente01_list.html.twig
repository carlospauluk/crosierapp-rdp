{% extends '@CrosierLibBase/list.html.twig' %}



{% block campos_pesquisar %}

    <div class="form-group">
        <label for="filter[codigo]">Código</label>
        <input type="text"
               class="form-control" id="filter[codigo]"
               name="filter[codigo]"
               value="{{ filter.codigo|default('') }}">
    </div>

    <div class="form-group">
        <label for="filter[nome]">Nome</label>
        <input type="text"
               class="form-control" id="filter[nome]"
               name="filter[nome]"
               value="{{ filter.nome|default('') }}">
    </div>

{% endblock %}


{% block down_scripts %}
    <script src="{{ asset('build/Relatorios/relCliente01_list.js') }}"></script>
{% endblock %}
